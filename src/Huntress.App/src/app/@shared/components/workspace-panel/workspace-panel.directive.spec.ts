import { WorkspacePanelDirective } from './workspace-panel.directive';

describe('WorkspacePanelDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkspacePanelDirective();
    expect(directive).toBeTruthy();
  });
});
