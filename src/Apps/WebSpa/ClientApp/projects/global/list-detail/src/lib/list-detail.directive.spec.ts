import { ListDetailDirective } from './list-detail.directive';

describe('ListDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ListDetailDirective();
    expect(directive).toBeTruthy();
  });
});
